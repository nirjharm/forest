language: cpp 
dist: trusty
os: linux
  addons:
    apt:
      sources:
        - ubuntu-toolchain-r-test
      packages:
        - g++-4.9
install:
  - sudo pip install codecov
script:
  - mkdir build
  - cd build
  - cmake ..
  - make