language: cpp 
dist: trusty
os:
  - linux
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-5.0
install:
  - sudo pip install codecov
script:
  - mkdir build
  - cd build
  - cmake .. -DBUILD_EXAMPLES=ON
  - make